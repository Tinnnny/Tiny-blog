#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_opencollective-postinstall@2.0.2@opencollective-postinstall/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_opencollective-postinstall@2.0.2@opencollective-postinstall/index.js" "$@"
  ret=$?
fi
exit $ret
